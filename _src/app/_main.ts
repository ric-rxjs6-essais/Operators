// import { Observable, interval } from "rxjs";
                                            
export class Main {
    public run(): void {
        // const oObservable: Observable<number> = interval(1000);
        // oObservable.subscribe( (piCounter: number) => {
        //     console.log(`${piCounter}`);
        // });
    }        

}